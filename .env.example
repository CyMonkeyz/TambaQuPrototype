# TambaQu currently needs no runtime secrets.
#
# VITE_ variables are bundled into public client JavaScript. Never place API
# keys, passwords, access tokens, or other secrets in a VITE_ variable.
# Future private configuration must be stored and enforced by a backend.
